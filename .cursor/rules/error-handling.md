# Error Handling

Never use try/catch unless needed.

Let errors propagate to global error middleware.

Create custom error classes.

Return standardized API errors.

Log unexpected errors.

Do not expose stack traces.

Validation errors should be handled separately.